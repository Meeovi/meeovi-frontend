mutation update_comments_item($id: ID!, $data: update_comments_input!){
    update_comments_item(id: $id, data: $data){
        id
        response
        name
        created_at
        updated_at
        helpful
        feedback
    }
}