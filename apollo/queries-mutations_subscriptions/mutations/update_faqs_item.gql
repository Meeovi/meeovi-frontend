mutation update_faqs_item($id: ID!, $data: update_faqs_input!){
    update_faqs_item(id: $id, data: $data){
        id
        status
        sort
        user_created
        user_updated
        question
        answer
    }
}