query cities_customers_aggregated($groupBy: [String], $filter: cities_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    cities_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            cities_id
            customers_id
        }
        countDistinct{
            id
            cities_id
            customers_id
        }
        avg{
            id
            cities_id
            customers_id
        }
        sum{
            id
            cities_id
            customers_id
        }
        avgDistinct{
            id
            cities_id
            customers_id
        }
        sumDistinct{
            id
            cities_id
            customers_id
        }
        min{
            id
            cities_id
            customers_id
        }
        max{
            id
            cities_id
            customers_id
        }
    }
}