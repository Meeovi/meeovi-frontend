query blocks_feature_card_by_version($version: String!, $id: ID!){
    blocks_feature_card_by_version(version: $version, id: $id){
        id
        title
        subtitle
        content
        image
    }
}