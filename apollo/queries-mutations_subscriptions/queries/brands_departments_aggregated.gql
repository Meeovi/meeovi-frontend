query brands_departments_aggregated($groupBy: [String], $filter: brands_departments_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    brands_departments_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            brands_id
            departments_id
        }
        countDistinct{
            id
            brands_id
            departments_id
        }
        avg{
            id
            brands_id
            departments_id
        }
        sum{
            id
            brands_id
            departments_id
        }
        avgDistinct{
            id
            brands_id
            departments_id
        }
        sumDistinct{
            id
            brands_id
            departments_id
        }
        min{
            id
            brands_id
            departments_id
        }
        max{
            id
            brands_id
            departments_id
        }
    }
}