query tags_by_version($version: String!, $id: ID!){
    tags_by_version(version: $version, id: $id){
        id
        status
        sort
        date_created
        name
        description
        image
    }
}