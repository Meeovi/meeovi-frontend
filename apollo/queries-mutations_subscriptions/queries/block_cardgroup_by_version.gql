query block_cardgroup_by_version($version: String!, $id: ID!){
    block_cardgroup_by_version(version: $version, id: $id){
        id
        headline
        content
        group_type
        posts
        cards
    }
}