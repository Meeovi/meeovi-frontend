query newsletters_by_version($version: String!, $id: ID!){
    newsletters_by_version(version: $version, id: $id){
        id
        email
        created_at
        optin
        name
        subject
        content
    }
}