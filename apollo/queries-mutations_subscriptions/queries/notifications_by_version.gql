query notifications_by_version($version: String!, $id: ID!){
    notifications_by_version(version: $version, id: $id){
        id
        date_created
        date_updated
        content
        is_read
        user_id
    }
}