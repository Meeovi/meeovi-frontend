query connections_by_version($version: String!, $id: ID!){
    connections_by_version(version: $version, id: $id){
        id
        date_created
        name
        description
        connection_type
        user_a
        recipient
        customers
    }
}