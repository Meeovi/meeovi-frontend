query states_by_id($id: ID!){
    states_by_id(id: $id){
        id
        name
        country_code
        fips_code
        iso2
        latitude
        longitude
        flag
        wikiDataId
        country_id
        created_at
        updated_at
    }
}