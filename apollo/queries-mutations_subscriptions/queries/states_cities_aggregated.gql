query states_cities_aggregated($groupBy: [String], $filter: states_cities_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    states_cities_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            states_id
            cities_id
        }
        countDistinct{
            id
            states_id
            cities_id
        }
        avg{
            id
            states_id
            cities_id
        }
        sum{
            id
            states_id
            cities_id
        }
        avgDistinct{
            id
            states_id
            cities_id
        }
        sumDistinct{
            id
            states_id
            cities_id
        }
        min{
            id
            states_id
            cities_id
        }
        max{
            id
            states_id
            cities_id
        }
    }
}