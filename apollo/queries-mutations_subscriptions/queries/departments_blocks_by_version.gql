query departments_blocks_by_version($version: String!, $id: ID!){
    departments_blocks_by_version(version: $version, id: $id){
        id
        departments_id
        item
        collection
    }
}