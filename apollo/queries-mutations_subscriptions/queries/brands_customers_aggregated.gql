query brands_customers_aggregated($groupBy: [String], $filter: brands_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    brands_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            brands_id
            customers_id
        }
        countDistinct{
            id
            brands_id
            customers_id
        }
        avg{
            id
            brands_id
            customers_id
        }
        sum{
            id
            brands_id
            customers_id
        }
        avgDistinct{
            id
            brands_id
            customers_id
        }
        sumDistinct{
            id
            brands_id
            customers_id
        }
        min{
            id
            brands_id
            customers_id
        }
        max{
            id
            brands_id
            customers_id
        }
    }
}