query careers_by_version($version: String!, $id: ID!){
    careers_by_version(version: $version, id: $id){
        id
        status
        sort
        date_created
        date_updated
        name
        url
        description
        type
        image
        degree_level
        experience
        customers
    }
}