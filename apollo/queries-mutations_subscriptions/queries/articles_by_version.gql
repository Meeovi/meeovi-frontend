query articles_by_version($version: String!, $id: ID!){
    articles_by_version(version: $version, id: $id){
        id
        name
        excerpt
        content
        created_at
        image
        type
        isPublic
        stamp
    }
}