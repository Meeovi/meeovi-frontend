query coupons_aggregated($groupBy: [String], $filter: coupons_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    coupons_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            name
            excerpt
            discount
            image
            expiration
            products
            customers
        }
        countDistinct{
            id
            name
            excerpt
            discount
            image
            expiration
            products
            customers
        }
        avg{
            id
            discount
        }
        sum{
            id
            discount
        }
        avgDistinct{
            id
            discount
        }
        sumDistinct{
            id
            discount
        }
        min{
            id
            discount
        }
        max{
            id
            discount
        }
    }
}