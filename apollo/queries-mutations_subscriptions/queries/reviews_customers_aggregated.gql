query reviews_customers_aggregated($groupBy: [String], $filter: reviews_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    reviews_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            customers_id
        }
        countDistinct{
            id
            customers_id
        }
        avg{
            id
            customers_id
        }
        sum{
            id
            customers_id
        }
        avgDistinct{
            id
            customers_id
        }
        sumDistinct{
            id
            customers_id
        }
        min{
            id
            customers_id
        }
        max{
            id
            customers_id
        }
    }
}