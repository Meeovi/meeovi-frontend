query currency_by_version($version: String!, $id: ID!){
    currency_by_version(version: $version, id: $id){
        id
        sort
        date_created
        date_updated
        name
        code
        symbol
        departments
        customers
        products
    }
}