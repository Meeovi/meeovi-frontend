query lists_customers_aggregated($groupBy: [String], $filter: lists_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    lists_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            lists_id
            customers_id
        }
        countDistinct{
            id
            lists_id
            customers_id
        }
        avg{
            id
            lists_id
            customers_id
        }
        sum{
            id
            lists_id
            customers_id
        }
        avgDistinct{
            id
            lists_id
            customers_id
        }
        sumDistinct{
            id
            lists_id
            customers_id
        }
        min{
            id
            lists_id
            customers_id
        }
        max{
            id
            lists_id
            customers_id
        }
    }
}