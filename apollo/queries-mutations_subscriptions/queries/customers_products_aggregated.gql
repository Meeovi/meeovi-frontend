query customers_products_aggregated($groupBy: [String], $filter: customers_products_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    customers_products_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            customers_id
            products_id
        }
        countDistinct{
            id
            customers_id
            products_id
        }
        avg{
            id
            customers_id
            products_id
        }
        sum{
            id
            customers_id
            products_id
        }
        avgDistinct{
            id
            customers_id
            products_id
        }
        sumDistinct{
            id
            customers_id
            products_id
        }
        min{
            id
            customers_id
            products_id
        }
        max{
            id
            customers_id
            products_id
        }
    }
}