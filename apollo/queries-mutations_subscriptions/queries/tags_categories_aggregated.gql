query tags_categories_aggregated($groupBy: [String], $filter: tags_categories_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    tags_categories_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            tags_id
            categories_id
        }
        countDistinct{
            id
            tags_id
            categories_id
        }
        avg{
            id
            tags_id
            categories_id
        }
        sum{
            id
            tags_id
            categories_id
        }
        avgDistinct{
            id
            tags_id
            categories_id
        }
        sumDistinct{
            id
            tags_id
            categories_id
        }
        min{
            id
            tags_id
            categories_id
        }
        max{
            id
            tags_id
            categories_id
        }
    }
}