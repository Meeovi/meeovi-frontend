query products_departments_aggregated($groupBy: [String], $filter: products_departments_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    products_departments_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            products_id
            departments_id
        }
        countDistinct{
            id
            products_id
            departments_id
        }
        avg{
            id
            products_id
            departments_id
        }
        sum{
            id
            products_id
            departments_id
        }
        avgDistinct{
            id
            products_id
            departments_id
        }
        sumDistinct{
            id
            products_id
            departments_id
        }
        min{
            id
            products_id
            departments_id
        }
        max{
            id
            products_id
            departments_id
        }
    }
}