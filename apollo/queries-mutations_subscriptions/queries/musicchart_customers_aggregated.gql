query musicchart_customers_aggregated($groupBy: [String], $filter: musicchart_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    musicchart_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            musicchart_id
            customers_id
        }
        countDistinct{
            id
            musicchart_id
            customers_id
        }
        avg{
            id
            musicchart_id
            customers_id
        }
        sum{
            id
            musicchart_id
            customers_id
        }
        avgDistinct{
            id
            musicchart_id
            customers_id
        }
        sumDistinct{
            id
            musicchart_id
            customers_id
        }
        min{
            id
            musicchart_id
            customers_id
        }
        max{
            id
            musicchart_id
            customers_id
        }
    }
}