query digiboard_customers_aggregated($groupBy: [String], $filter: digiboard_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    digiboard_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            digiboard_id
            customers_id
        }
        countDistinct{
            id
            digiboard_id
            customers_id
        }
        avg{
            id
            digiboard_id
            customers_id
        }
        sum{
            id
            digiboard_id
            customers_id
        }
        avgDistinct{
            id
            digiboard_id
            customers_id
        }
        sumDistinct{
            id
            digiboard_id
            customers_id
        }
        min{
            id
            digiboard_id
            customers_id
        }
        max{
            id
            digiboard_id
            customers_id
        }
    }
}