query newsfeed_customers_aggregated($groupBy: [String], $filter: newsfeed_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    newsfeed_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            newsfeed_id
            customers_id
        }
        countDistinct{
            id
            newsfeed_id
            customers_id
        }
        avg{
            id
            newsfeed_id
            customers_id
        }
        sum{
            id
            newsfeed_id
            customers_id
        }
        avgDistinct{
            id
            newsfeed_id
            customers_id
        }
        sumDistinct{
            id
            newsfeed_id
            customers_id
        }
        min{
            id
            newsfeed_id
            customers_id
        }
        max{
            id
            newsfeed_id
            customers_id
        }
    }
}