query manufacturer_by_version($version: String!, $id: ID!){
    manufacturer_by_version(version: $version, id: $id){
        id
        created_at
        code
        name
        description
        isPublic
        image
    }
}