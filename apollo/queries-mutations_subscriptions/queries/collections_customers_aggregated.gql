query collections_customers_aggregated($groupBy: [String], $filter: collections_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    collections_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            collections_id
            customers_id
        }
        countDistinct{
            id
            collections_id
            customers_id
        }
        avg{
            id
            collections_id
            customers_id
        }
        sum{
            id
            collections_id
            customers_id
        }
        avgDistinct{
            id
            collections_id
            customers_id
        }
        sumDistinct{
            id
            collections_id
            customers_id
        }
        min{
            id
            collections_id
            customers_id
        }
        max{
            id
            collections_id
            customers_id
        }
    }
}