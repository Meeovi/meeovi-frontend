query newsfeed_by_version($version: String!, $id: ID!){
    newsfeed_by_version(version: $version, id: $id){
        id
        status
        sort
        user_created
        date_created
        user_updated
        date_updated
        post
        image
    }
}