query block_image_carousel_by_version($version: String!, $id: ID!){
    block_image_carousel_by_version(version: $version, id: $id){
        id
        title
        text
        url
        media
    }
}