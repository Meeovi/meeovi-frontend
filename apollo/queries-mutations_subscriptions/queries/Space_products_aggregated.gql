query Space_products_aggregated($groupBy: [String], $filter: Space_products_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    Space_products_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            Space_id
            products_id
        }
        countDistinct{
            id
            Space_id
            products_id
        }
        avg{
            id
            Space_id
            products_id
        }
        sum{
            id
            Space_id
            products_id
        }
        avgDistinct{
            id
            Space_id
            products_id
        }
        sumDistinct{
            id
            Space_id
            products_id
        }
        min{
            id
            Space_id
            products_id
        }
        max{
            id
            Space_id
            products_id
        }
    }
}