query followers_by_version($version: String!, $id: ID!){
    followers_by_version(version: $version, id: $id){
        id
        status
        sort
        date_created
        date_updated
        follower_id
        following_id
    }
}