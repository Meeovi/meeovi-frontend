query shipments_by_version($version: String!, $id: ID!){
    shipments_by_version(version: $version, id: $id){
        product
        speed_grade
        ship_date
        carrier_name
        transit_time
        tracking_url
        image
        id
        client_id
        client_secret
        country
        website
    }
}