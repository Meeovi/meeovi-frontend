query translations_customers_aggregated($groupBy: [String], $filter: translations_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    translations_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            translations_id
            customers_id
        }
        countDistinct{
            id
            translations_id
            customers_id
        }
        avg{
            id
            translations_id
            customers_id
        }
        sum{
            id
            translations_id
            customers_id
        }
        avgDistinct{
            id
            translations_id
            customers_id
        }
        sumDistinct{
            id
            translations_id
            customers_id
        }
        min{
            id
            translations_id
            customers_id
        }
        max{
            id
            translations_id
            customers_id
        }
    }
}