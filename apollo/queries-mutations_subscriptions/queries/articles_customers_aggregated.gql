query articles_customers_aggregated($groupBy: [String], $filter: articles_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    articles_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            articles_id
            customers_id
        }
        countDistinct{
            id
            articles_id
            customers_id
        }
        avg{
            id
            articles_id
            customers_id
        }
        sum{
            id
            articles_id
            customers_id
        }
        avgDistinct{
            id
            articles_id
            customers_id
        }
        sumDistinct{
            id
            articles_id
            customers_id
        }
        min{
            id
            articles_id
            customers_id
        }
        max{
            id
            articles_id
            customers_id
        }
    }
}