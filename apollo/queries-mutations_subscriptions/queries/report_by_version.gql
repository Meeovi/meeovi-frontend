query report_by_version($version: String!, $id: ID!){
    report_by_version(version: $version, id: $id){
        id
        sort
        user_created
        date_created
        user_updated
        date_updated
        title
        content
    }
}