query translations_customers_by_version($version: String!, $id: ID!){
    translations_customers_by_version(version: $version, id: $id){
        id
        translations_id
        customers_id
    }
}