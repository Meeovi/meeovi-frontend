query websites_customers_aggregated($groupBy: [String], $filter: websites_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    websites_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            websites_id
            customers_id
        }
        countDistinct{
            id
            websites_id
            customers_id
        }
        avg{
            id
            websites_id
            customers_id
        }
        sum{
            id
            websites_id
            customers_id
        }
        avgDistinct{
            id
            websites_id
            customers_id
        }
        sumDistinct{
            id
            websites_id
            customers_id
        }
        min{
            id
            websites_id
            customers_id
        }
        max{
            id
            websites_id
            customers_id
        }
    }
}