query departments_customers_aggregated($groupBy: [String], $filter: departments_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    departments_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            departments_id
            customers_id
        }
        countDistinct{
            id
            departments_id
            customers_id
        }
        avg{
            id
            departments_id
            customers_id
        }
        sum{
            id
            departments_id
            customers_id
        }
        avgDistinct{
            id
            departments_id
            customers_id
        }
        sumDistinct{
            id
            departments_id
            customers_id
        }
        min{
            id
            departments_id
            customers_id
        }
        max{
            id
            departments_id
            customers_id
        }
    }
}