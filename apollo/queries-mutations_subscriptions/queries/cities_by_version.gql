query cities_by_version($version: String!, $id: ID!){
    cities_by_version(version: $version, id: $id){
        id
        name
        description
        postalCode
        image
        longitude
        latitude
        languagenames
        country
        customers
        states
    }
}