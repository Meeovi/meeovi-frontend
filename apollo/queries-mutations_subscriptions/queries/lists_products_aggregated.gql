query lists_products_aggregated($groupBy: [String], $filter: lists_products_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    lists_products_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            lists_id
            products_id
        }
        countDistinct{
            id
            lists_id
            products_id
        }
        avg{
            id
            lists_id
            products_id
        }
        sum{
            id
            lists_id
            products_id
        }
        avgDistinct{
            id
            lists_id
            products_id
        }
        sumDistinct{
            id
            lists_id
            products_id
        }
        min{
            id
            lists_id
            products_id
        }
        max{
            id
            lists_id
            products_id
        }
    }
}