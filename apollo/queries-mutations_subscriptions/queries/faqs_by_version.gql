query faqs_by_version($version: String!, $id: ID!){
    faqs_by_version(version: $version, id: $id){
        id
        status
        sort
        user_created
        user_updated
        question
        answer
    }
}