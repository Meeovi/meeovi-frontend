query currency_customers_aggregated($groupBy: [String], $filter: currency_customers_filter, $limit: Int, $offset: Int, $page: Int, $search: String, $sort: [String]){
    currency_customers_aggregated(groupBy: $groupBy, filter: $filter, limit: $limit, offset: $offset, page: $page, search: $search, sort: $sort){
        group
        countAll
        count{
            id
            currency_id
            customers_id
        }
        countDistinct{
            id
            currency_id
            customers_id
        }
        avg{
            id
            currency_id
            customers_id
        }
        sum{
            id
            currency_id
            customers_id
        }
        avgDistinct{
            id
            currency_id
            customers_id
        }
        sumDistinct{
            id
            currency_id
            customers_id
        }
        min{
            id
            currency_id
            customers_id
        }
        max{
            id
            currency_id
            customers_id
        }
    }
}