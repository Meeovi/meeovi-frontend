query warehouse_by_version($version: String!, $id: ID!){
    warehouse_by_version(version: $version, id: $id){
        id
        created_at
        name
        description
        city
        state
        country
        postal
        status
        image
        products
        category
        isPublic
    }
}