diff --git a/node_modules/@vite-pwa/nuxt/dist/shared/nuxt.9518178d.mjs b/node_modules/@vite-pwa/nuxt/dist/shared/nuxt.9518178d.mjs
index 5879ae4..e65bd67 100644
--- a/node_modules/@vite-pwa/nuxt/dist/shared/nuxt.9518178d.mjs
+++ b/node_modules/@vite-pwa/nuxt/dist/shared/nuxt.9518178d.mjs
@@ -247,6 +247,9 @@ function addPWAIconsPluginTemplate(pwaAssetsEnabled) {
       getContents: () => `// Generated by @vite-pwa/nuxt
 import { defineNuxtPlugin } from '#imports'
 import { pwaAssetsIcons } from 'virtual:pwa-assets/icons'
+// @ts-ignore -- vue-tsc's module resolution doesn't resolve this path-mapped
+// virtual module the same way plain tsc does; the generated .d.ts is real
+// and present on disk.
 import type { PWAAssetIcon, PWAIcons } from '#build/pwa-icons/pwa-icons'
 
 export default defineNuxtPlugin(() => {
@@ -264,12 +267,12 @@ export default defineNuxtPlugin(() => {
 })
 
 function configureEntry<K extends keyof PWAIcons>(key: K) {
-  return Object.values(pwaAssetsIcons[key] ?? {}).reduce((acc, icon) => {
+  return Object.values((pwaAssetsIcons as any)[key] ?? {}).reduce((acc: any, icon: any) => {
     const entry: PWAAssetIcon<any> = {
       ...icon,
       asImage: {
         src: icon.url,
-        key: \`\${key}-\${icon.name}\`
+        key: \`\${String(key)}-\${icon.name}\`
       }
     }
     if (icon.width && icon.height) {
