mutation addCommentToPost($pageSize: Int, $currentPage: Int, $input: addCommentToPostInput){
    addCommentToPost(pageSize: $pageSize, currentPage: $currentPage, input: $input){
        comments{
            admin_id
            author_email
            author_nickname
            author_type
            comment_id
            creation_time
            customer_id
            parent_id
            post_id
            replies{
                admin_id
                author_email
                author_nickname
                author_type
                comment_id
                creation_time
                customer_id
                parent_id
                post_id
                replies{
                    admin_id
                    author_email
                    author_nickname
                    author_type
                    comment_id
                    creation_time
                    customer_id
                    parent_id
                    post_id
                    status
                    text
                    update_time
                }
                status
                text
                update_time
            }
            status
            text
            update_time
        }
        total_count
        total_pages
    }
}